# Never upload secrets or the local corpus to Vercel's build.
# (Set env vars in the Vercel project settings instead.)
.env*
/data/
.claude/
agent/

# Vercel's builder currently rejects this npm-11 lockfile with "Invalid Version".
# Resolve from package.json; Moss's Linux binding is declared there explicitly.
package-lock.json
